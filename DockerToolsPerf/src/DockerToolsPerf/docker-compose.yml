version: '2'

services:
  dockertoolsperf:
    image: weidazhao/dockertoolsperf
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_URLS=http://*:5000
    ports:
      - "80:5000"
